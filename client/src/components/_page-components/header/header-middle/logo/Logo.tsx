import React from 'react';
import css from './Logo.module.scss';

const Logo = () => {
    return (
        <div className={css.container}>
            MARKET
        </div>
    );
};

export default Logo;